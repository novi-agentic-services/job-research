# job-research dashboard

Mobile-friendly static dashboard for insurer job tracking.

## Cloudflare Pages deployment
- Framework preset: **None**
- Build command: *(empty)*
- Build output directory: `/`

## Files
- `index.html` - dashboard UI
- `styles.css` - responsive styling
- `app.js` - CSV parsing + filters + rendering
- `insurer_jobs_available.csv` - data source
